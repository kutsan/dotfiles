module.exports = {
	// Default port.
	port: 3000,

	// Default UI port.
	ui: { port: 3001 },

	// Watch all files from the current directory.
	files: '*',

	// Serve static files from the current directory.
	server: true,

	// Disable small pop-over notifications.
	notify: false,

	// Only serve files through localhost.
	online: false,

	// Disable clicks, scrolls and form inputs sync.
	ghostMode: false,

	// Disable defult "[Browsersync]" log prefix.
	logPrefix: null
}
