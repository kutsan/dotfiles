# Aliases
.alias $left_bracket 0x21 # `[` key (kVK_ANSI_LeftBracket)
.alias $right_bracket 0x1E # `]` key (kVK_ANSI_RightBracket)

# History navigation via mouse side buttons
mouse4 | cmd - $left_bracket # back (cmd + [)
mouse5 | cmd - $right_bracket # forward (cmd + ])
