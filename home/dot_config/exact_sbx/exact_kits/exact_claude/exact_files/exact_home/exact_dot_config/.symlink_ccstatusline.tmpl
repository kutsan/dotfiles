# Documentation only. Disabled because `sbx kit pack` rejects symlinks that
# escape the kit directory, so `run_onchange_after_sync-sbx-kit-files.sh` copies
# the target below into this directory instead of linking to it. The leading dot
# stops `chezmoi` from rendering this file.
{{ .chezmoi.homeDir }}/.config/ccstatusline
